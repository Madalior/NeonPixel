name: Auto Update NeonPixel

on:
  schedule:
    - cron: "0 */12 * * *"   # Runs every 12 hours
  workflow_dispatch:         # Allows manual run button

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write        # Critical for saving files
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install -r requirements.txt

      - name: Install FFmpeg
        run: sudo apt-get install -y ffmpeg

      # --- FIX IS HERE: Pull latest changes BEFORE running the bot ---
      - name: Sync with GitHub
        run: git pull origin main || echo "Nothing to pull"

      - name: Run AI Cloud Bot
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          WALLHAVEN_API_KEY: ${{ secrets.WALLHAVEN_API_KEY }}
        run: python cloud_bot.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name "NeonPixel AI"
          git config --global user.email "bot@neonpixel.com"
          
          # Safety Net: Create files if they are missing
          if [ ! -f data/cloud_wallpapers.json ]; then
            mkdir -p data
            echo "[]" > data/cloud_wallpapers.json
          fi
          if [ ! -f data/videos.json ]; then
            mkdir -p data
            echo "[]" > data/videos.json
          fi
          
          git add data/cloud_wallpapers.json data/videos.json
          
          # Only commit if there are actual changes
          git diff-index --quiet HEAD || git commit -m "🤖 AI Auto-Update: New Assets"
          
          # Force push to resolve any remaining conflicts
          git push origin main