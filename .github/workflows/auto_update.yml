name: Auto Update NeonPixel

on:
  schedule:
    - cron: "0 */12 * * *"   # Runs every 12 hours
  workflow_dispatch:         # Allows manual run button

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # FIX 1: Install 'cloudinary' from your requirements.txt
      - name: Install Libraries
        run: pip install -r requirements.txt

      # FIX 2 & 3: Run 'cloud_bot.py' and pass the API Keys
      - name: Run AI Cloud Bot
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
        run: python cloud_bot.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name "NeonPixel AI"
          git config --global user.email "bot@neonpixel.com"
          # We only need to save these 2 JSON files
          git add data/cloud_wallpapers.json data/videos.json
          git commit -m "🤖 AI Auto-Update: New Assets" || echo "No changes to commit"
          git push
