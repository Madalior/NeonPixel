name: Auto Update NeonPixel

on:
  schedule:
    - cron: "0 */6 * * *"   # Runs every 12 hours
  workflow_dispatch:         # Allows manual run button

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write        # Critical for saving data
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0     # Fetch full history for git syncing

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install -r requirements.txt

      - name: Install FFmpeg
        run: sudo apt-get install -y ffmpeg

      - name: Configure Git
        run: |
          git config --global user.name "NeonPixel AI"
          git config --global user.email "bot@neonpixel.com"

      - name: Run AI Cloud Bot
        env:
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          WALLHAVEN_API_KEY: ${{ secrets.WALLHAVEN_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python cloud_bot.py